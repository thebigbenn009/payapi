import { ReactElement } from "react";

export type Goal = {
  icon: ReactElement;
  label: string;
  text: string;
};
export type LinkType = {
  href: string;
  label: string;
};
export type Icons = {
  icon: ReactElement;
};
